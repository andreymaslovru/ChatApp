import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '70%',
    backgroundColor: 'white',
    paddingVertical: 4,
    borderRadius: 16,
    fontSize: 16
  },
});
